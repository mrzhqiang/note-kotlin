/** 4 类、对象和接口 */

// Kotlin的声明默认是final 和public 的

// 嵌套的类默认并不是内部类：没有包含对外部类的隐式引用

// 对于构造方法和属性来说，简洁的方式大部分情况下都工作得很好，但你也可以自定义它们来实现更丰富的逻辑

// 将一个类声明为data 类可以让编译器为这个类生成若干标准方法。同样可以避免动手书写委托方法，因为Kotlin原生支持。

